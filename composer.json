{
    "name": "myclient/whalebooks-client",
    "description": "A client library for communicating with the WhaleBooks REST API",
    "type": "library",
    "require": {
        "guzzlehttp/guzzle": "^7.8"
    },
    "autoload": {
        "psr-4": {
            "MyClient\\WhaleBooksClient\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^11.1"
    },
    "scripts": {
        "test": "phpunit --bootstrap vendor/autoload.php tests"
    }
}
